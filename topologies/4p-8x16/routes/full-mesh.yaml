# full-mesh.yaml — every host talks to every other host of the same tenant.
#
# This expands to:
#   16 hosts * 15 peers / 2  = 120 unique pairs per tenant
#   120 pairs * 2 tenants    = 240 pairs total
#   240 pairs * 8 routes/pair = 1920 routes
#
# Use this to stress-test the kernel route table, validate spray.py across
# arbitrary host combinations, or just have everything reachable for ad-hoc
# pings. Spine selection is auto: the 8 reference pairs match the original
# PAIRS table for reproducibility, everything else falls through to a hash
# (a * 16 + b) % 8.
#
# Apply with:
#   ./routes.py apply -f routes/full-mesh.yaml
apiVersion: srv6-lab/v1
kind: RouteSet
metadata:
  name: full-mesh
spec:
  mesh:
    - tenant: green
      src: all
      dst: all
      # self-pairs (src == dst) are dropped automatically
      # the reverse pair (a->b vs b->a) is canonicalized and deduped
    - tenant: yellow
      src: all
      dst: all
